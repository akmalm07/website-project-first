document.addEventListener('DOMContentLoaded', () => {

    const scene = document.getElementById('load-intro-scene');
  
    setTimeout(() => {
      scene.classList.add('fade-out');
    }, 1800); 
  });